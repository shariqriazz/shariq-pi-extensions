name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: jdx/mise-action@v3
        with:
          cache: true
      - run: mise exec --locked -- npm ci
      - run: mise exec --locked -- npm run validate
      - run: mise exec --locked -- npm run pack:inspect
